{
  "name": "expo-template",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile:dev": "npm run dev --workspace=mobile",
    "server:dev": "npm run dev --workspace=server"
  }
}
